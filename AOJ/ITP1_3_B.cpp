#include <bits/stdc++.h>
#define log(x) cout<<x<<endl
#define pb push_back
#define rep(i,n) for(int i = 0;i < n;++i)
using namespace std;

int main() {
    int a,b;
    vector<long long> li;
    b=0;
    while(true){
        cin >> a;
        if(a==0) break;
        b++;
        log("Case "+to_string(b)+": "+to_string(a));
    }
}