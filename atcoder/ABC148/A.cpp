#include <bits/stdc++.h>
#define log(x) cerr<<x<<endl
#define pb push_back
#define rep(i,n) for(int i = 0;i < n;++i)
using namespace std;

int main() {
    int a,b;
    cin >> a;cin >> b;
    if(a==1&&b==2)log(3);
    if(a==1&&b==3)log(2);
    if(a==2&&b==3)log(1);
}